# Uptime Kuma Docker Compose configuration

# Pin the image tag for reproducible deployments.
# Examples: 1 (tracks latest v1), 1.23.0 (specific release)
KUMA_TAG=1

# Host port that maps to container port 3001 (UI)
KUMA_HOST_PORT=8010

# Optional: set timezone inside the container
# TZ=UTC

# Optional: set a Compose project name (namespaces containers/networks)
# COMPOSE_PROJECT_NAME=uptime-kuma

# Multi-instance ports (docker-compose.override.yml)
KUMA_HOST_PORT_A=8010
KUMA_HOST_PORT_B=8011
KUMA_HOST_PORT_C=8012
# KUMA_HOST_PORT_D=8013
